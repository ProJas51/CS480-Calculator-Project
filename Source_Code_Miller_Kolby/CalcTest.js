console.log("This program will generate automatic test cases for the calculator program created in lab 3."); 


const isValid = true; 

let testCase = []; 

// this function will take in an array as the generated expression, and add either a number, operator, or parentheses to the expression 
function fillExpression() { 

    // for loop to update the expression array 
    for (let i = 0; i < testCase.length; i++) { 
        
        // traverse calc_buttons 
        for (let j = 0; j < calc_buttons.length; j++) { 
            
            // everytime a valid character is met, compare to the value in expression 
            calc_buttons[j]; 
        }

        if (testCase[i] == calc_buttons[j]) { 

        }

    }
    
}

function evalExpression() { 

    console.log(eval(testCase)); 

} 